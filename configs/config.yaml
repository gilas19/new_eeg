defaults:
  - model: cnn
  - data: default
  - _self_

task: right_left
seed: 42
device: cuda

wandb:
  enabled: false
  project: eeg-classification
  entity: null
  mode: online

training:
  epochs: 100
  patience: 15
  learning_rate: 0.0001
  weight_decay: 0.00001
