defaults:
  - model: cnn
  - preprocessing: default
  - training: default
  - _self_

# General settings
task: right_left
seed: 42
device: cuda

# Data settings
data:
  data_dir: ./data/09_25_7
  batch_size: 32
  val_split: 0.2

# Experiment tracking
wandb:
  enabled: false
  project: eeg-classification
  entity: null
  mode: online
