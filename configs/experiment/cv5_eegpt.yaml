# @package _global_

defaults:
  - override /data: cv5
  - override /wandb: default
  - override /training: default
  - override /task: free_instructed
  - override /model: eegpt
  - override /preprocessing: default

task: free_instructed
seed: 42
device: cuda
