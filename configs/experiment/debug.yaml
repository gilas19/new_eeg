# @package _global_

defaults:
  - override /data: default
  - override /wandb: disabled
  - override /training: default
  - override /task: simple_trials
  - override /model: cnn
  - override /preprocessing: default

task: simple_trials
seed: 42
device: cuda

training:
  epochs: 3
  patience: 2
  learning_rate: 0.0001
  weight_decay: 0.00001
  scheduler:
    enabled: false
